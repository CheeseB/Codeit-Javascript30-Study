class SoundKeyInfo {
    constructor(keyInfo, soundInfo, soundUrl) {
      this.keyInfo = keyInfo;
      this.soundInfo = soundInfo;
      this.soundUrl = soundUrl;
    }
  }

  
export default SoundKeyInfo;